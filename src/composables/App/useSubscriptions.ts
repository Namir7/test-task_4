// import { useStore } from "@/store";

export const useSubscriptions = () => {
  // const store = useStore();
  // store.subscribe((mutation, state) => {
  //   const interestedMutations = ["nodes/CHANGE_ACTIVE"];
  //   //   saves previous version of state.nodesList value in bin
  //   if (interestedMutations.includes(mutation.type)) {
  //     console.log("subscription");
  //     store.commit("edit/CHANGE_VALUE", {
  //       value: state.nodes.activeNode.label,
  //     });
  //   }
  // });
};
