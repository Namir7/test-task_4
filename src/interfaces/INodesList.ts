// import { INode } from "./INode";

// export interface INodesList {
// nodes: INode[];
// addNode(label: string, parentId: string): INode;
// removeNode(id: string, parentId: string): INode | null;
// }
